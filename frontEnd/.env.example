# Set this in Vercel to the public HTTPS URL of the deployed FastAPI service.
VITE_API_URL=https://your-api-domain.example.com
# HTTPS widget URL pattern; deploy all category services or route these ports on your proxy.
VITE_WIDGET_URL_TEMPLATE=https://widgets.example.com:{port}/{botId}
