# ── Core API ──────────────────────────────────────────────────────
fastapi==0.104.1
uvicorn[standard]==0.24.0
gunicorn==22.0.0
python-multipart==0.0.6

# ── Database ───────────────────────────────────────────────────────
sqlalchemy==2.0.23
pymysql==1.1.0

# ── Pydantic / Settings ────────────────────────────────────────────
pydantic>=2.7.4,<3.0.0
pydantic-settings==2.1.0

# ── Auth ───────────────────────────────────────────────────────────
python-dotenv==1.0.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.1.0

# ── Pinecone / Embeddings (university + restaurant pipelines) ──────
pinecone
langchain-pinecone
langchain-openai
langchain-core
openai

# ── PDF extraction — university pipeline ──────────────────────────
pypdf
pdfplumber

# ── PDF extraction — restaurant pipeline (GPT-4o vision) ──────────
pymupdf          # fitz
pillow           # PIL
pandas

# ── OCR fallback (optional — install Tesseract + Poppler separately) ──
# pdf2image
# pytesseract
